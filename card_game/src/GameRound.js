// Один раунд простой карточной мини-игры «High Sum»:
// каждому игроку сдаётся по 2 карты, побеждает игрок с наибольшей суммой.
// Ничья возможна.

import Deck from './Deck.js';

export default class GameRound {
  constructor(players, deck = new Deck()) {
    // TODO: сохраните массив players, проверьте что их >=2
    // TODO: сохраните deck и, например, сразу перемешайте его
  }

  /** Сдать каждому игроку по n карт (по умолчанию 2) */
  deal(n = 2) {
    // TODO: цикл по игрокам + цикл по количеству карт; используйте методы Player.drawFrom
  }

  /** Вернуть имя победителя или строку 'draw' при ничьей */
  computeWinner() {
    // TODO: вычислите максимальное значение руки, проверьте несколько максимумов
  }

  /** Провести полный раунд: сдача, подсчёт, начисление очков, возврат карт в низ колоды */
  play(n = 2) {
    // TODO: вызовите deal(n), найдите победителя, начислите очки победителю,
    // затем попросите игроков discartTo(deck). Верните объект результата:
    // { winnerName: string|'draw' }
  }
}